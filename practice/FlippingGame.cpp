#include<bits/stdc++.h>
using namespace std;

void solve(){
    int n;
    cin >> n;
    vector<int> arr;
    hello how are keeping up with the cube?
    arr.resize(n);
    for(int i = 0; i < )
}

int main() {
    solve();
    return 0;
}