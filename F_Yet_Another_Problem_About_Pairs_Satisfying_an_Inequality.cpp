// Hare Krishna
// Code by Prankur Sharma
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <map>
#include <unordered_map>
#include <set>
#include <unordered_set>
#include <queue>
#include <stack>
#include <deque>
#include <cmath>
#include <climits>
using namespace std;

#define int long long
#define fast ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);

// Observation
// ai < i < aj < j
// we can also write it as 
// ai - i < aj - j

// We will take two number jo apne index se chote hoo plus unke aage unsai koi bada hoo 

// Approach woule be we need to know how many values are there after each value which is smaller than its index such that tey are bigger than 

// first we will make a pair of <idx , val>
// we will sort them 
// we will sort 
void solve(){
    
}

int32_t main(){
    fast;
    int t = 1;
    cin >> t;
    while(t--){
        solve();
    }
    return 0;
}