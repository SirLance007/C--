#include<bits/stdc++.h>
using namespace std;

void solve(){
    int n;
    cin >> n;
    vector<int> arr1;
    vector<int> arr2;
    for(int i = 0; i < n; i++){
        int x;
        cin >> x;
        arr1.push_back(x);
    }
    for(int i = 0; i < n; i++){
        int x;
        cin >> x;
        arr2.push_back(x);
    }
    for(int i = 0 ; i < n ; i++){
        if(arr1[i] == arr2[])
    }
}

int mian(){
    int t;
    cin >> t;
    while(t-- >0){
        solve();
    }
}