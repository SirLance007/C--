// Hare Krishna
// Code by Prankur Sharma
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <map>
#include <unordered_map>
#include <set>
#include <unordered_set>
#include <queue>
#include <stack>
#include <deque>
#include <cmath>
#include <climits>
using namespace std;

#define int long long
#define fast ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);

// Observations
// operations - 1
// revrse a seqeuence
// operation - 2
// replace it with its diffrence 
// so operation - 2 we need to do to reduce size
// now we need maximum possible sum
// that means 
// so for maximum diffrence 
// we need first one neg and then one pos

// If all positive no need to do that further
// Until a single negative come make diffrence

// Test case
// 9 7 9 -9 9 -8 7 -8 9
// -9 -8 -8 7 7 9 9 9 9
// max sum -> 18 + 17 + 17 + 17 + 16 + 7


void solve(){
    
}

int32_t main(){
    fast;
    int t = 1;
    cin >> t;
    while(t--){
        solve();
    }
    return 0;
}