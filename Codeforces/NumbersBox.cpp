#include<bits/stdc++.h>
using namespace std;

void solve(){
    int n , m;
    cin >> n >> m;
    vector<int> ans;
    for(int i = 0; i < n ; i++){
        for(int j = 0 ; j < m ; j++){
            int x;
            cin >> x;
            ans.push_back(x);
        }
    }
    for(int i = 0; i < n ; i++){
        for(int j = 0 ;  )
    }
}